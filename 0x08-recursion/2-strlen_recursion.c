#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen_recursion - A function that returns the length of a string
 * @s: string
 *
 * Return: Renturns an int
 */

int _strlen_recursion(char *s)
{
	return (strlen(s));
}
